<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yzhqnyzwb.inventorypro">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/barcode_scan"
        android:label="@string/app_name"
        android:roundIcon="@drawable/barcode_scan"
        android:screenOrientation="portrait"
        android:supportsRtl="false"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BarcodeList" />
        <activity android:name=".BarcodeCaptureActivity" />
        >
        <activity android:name=".ProfitActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".SerchOfDataBase" />
        <activity android:name=".RagisterUser" />
        <activity android:name=".ZedImportList" />
        <activity android:name=".MengerProdctsActivity"></activity>
    </application>

</manifest>